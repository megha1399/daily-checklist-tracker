export const environment = {
  /**
   * Base path for the routine API (e.g. `/api` with `ng serve` + proxy, or `https://your.host/api`).
   * Set to `null` to use browser storage only (no HTTP sync).
   */
  apiBaseUrl: '/api' as string | null,
};
